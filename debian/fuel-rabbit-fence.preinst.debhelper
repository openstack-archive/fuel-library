# Automatically added by dh_installinit
if [ "$1" = install ] || [ "$1" = upgrade ]; then
	if [ -e "/etc/init.d/fuel-rabbit-fence" ] && [ -L "/etc/init.d/fuel-rabbit-fence" ] \
	   && [ $(readlink -f "/etc/init.d/fuel-rabbit-fence") = /lib/init/upstart-job ]
	then
		rm -f "/etc/init.d/fuel-rabbit-fence"
	fi
fi
# End automatically added section
