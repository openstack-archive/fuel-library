# Force everything except cobbler and nailgun dir to port 443
RewriteEngine on
RewriteCond %{HTTPS} off
RewriteCond %{REQUEST_URI} !^/(cblr|cobbler)
RewriteRule (.*) https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]
