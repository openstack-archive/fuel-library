- id: ssl-keys-saving
  type: puppet
  groups: [primary-controller, controller]
  requires: [firewall]
  condition: "(settings:ssl.horizon.value == true or settings:ssl.services.value == true) and settings:ssl.cert_source.value == 'user_uploaded'"
  required_for: [deploy_end]
  parameters:
    puppet_manifest: /etc/puppet/modules/osnailyfacter/modular/ssl/ssl_keys_saving.pp
    puppet_modules: /etc/puppet/modules
    timeout: 3600
  test_pre:
    cmd: python /etc/puppet/modules/osnailyfacter/modular/ssl/ssl_keys_saving_pre.rb
