<task>
- id: heat
  type: puppet
  groups: [primary-controller, controller]
  required_for: [deploy, top-role-controller]
  requires: [hiera, globals, netconfig, firewall]
  parameters:
  puppet_manifest: /etc/puppet/modules/osnailyfacter/modular/heat.pp
  puppet_modules: /etc/puppet/modules
  timeout: 3600
</task>
