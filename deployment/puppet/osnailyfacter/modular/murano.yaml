---
- id: murano
  type: puppet
  groups: [primary-controller, controller]
  required_for: [deploy]
  requires: [hiera, globals, netconfig, firewall, top-role-controller]
  parameters:
  puppet_manifest: /etc/puppet/modules/osnailyfacter/modular/murano.pp
  puppet_modules: /etc/puppet/modules
  timeout: 3600
