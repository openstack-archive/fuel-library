- id: hiera
  type: puppet
  version: 2.0.0
  groups: [primary-controller, controller, cinder, cinder-block-device,
    cinder-vmware, compute, ceph-osd, primary-mongo, mongo, virt, ironic]
  requires: [deploy_start]
  required_for: [setup_repositories]
  parameters:
    puppet_manifest: /etc/puppet/modules/osnailyfacter/modular/hiera/hiera.pp
    puppet_modules: /etc/puppet/modules
    timeout: 3600
  test_pre:
    cmd: python /etc/puppet/modules/osnailyfacter/modular/hiera/hiera_pre.py
  test_post:
    cmd: python /etc/puppet/modules/osnailyfacter/modular/hiera/hiera_post.py

# - id: facter_cache_save
#   type: puppet
#   groups: [primary-controller, controller, cinder, cinder-vmware, compute, ceph-osd, zabbix-server, primary-mongo, mongo, virt]
#   requires: [deploy_start]
#   required_for: [hiera, deploy_end]
#   parameters:
#     puppet_manifest: /etc/puppet/modules/osnailyfacter/modular/hiera/facter_cache_save.pp
#     puppet_modules: /etc/puppet/modules
#     timeout: 3600

# - id: facter_cache_delete
#   type: shell
#   groups: [primary-controller, controller, cinder, cinder-vmware, compute, ceph-osd, zabbix-server, primary-mongo, mongo, virt]
#   requires: []
#   required_for: []
#   parameters:
#     cmd: /bin/rm -f /etc/facter/facts.d/cache.yaml
#     timeout: 30
