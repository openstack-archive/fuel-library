fixtures:
    repositories:
        'neutron':
            repo: 'http://review.fuel-infra.org/p/puppet-modules/puppet-neutron.git'
            ref:  '7.0.0-mos-rc2'
    symlinks:
        'osnailyfacter': "#{source_dir}"
        'l23network': "#{source_dir}/../l23network"
