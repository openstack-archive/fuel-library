- id: updates-neutron
  type: puppet
  groups: [primary-controller, controller, compute]
  required_for: []
  requires: []
  parameters:
    puppet_manifest: /etc/puppet/modules/updates/modular/neutron/neutron.pp
    puppet_modules: /etc/puppet/modules
    timeout: 3600
