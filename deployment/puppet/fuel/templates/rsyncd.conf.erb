address         = <%= @bind_address %>

[puppet]
path            = <%= @puppet_folder %>
read only       = true
uid             = 0
gid             = 0
use chroot      = no

[plugins]
path            = /var/www/nailgun/plugins
read only       = true
uid             = 0
gid             = 0
use chroot      = no

&include <%= @rsync_config_dir %>
