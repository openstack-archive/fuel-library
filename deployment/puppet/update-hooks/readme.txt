These files will be executes by Astute:

/etc/puppet/modules/update-hooks/post-deploy.rb -> After each role one every node
/etc/puppet/modules/update-hooks/pre-deploy.rb  -> Before each role on every node
/etc/puppet/modules/update-hooks/post-node.rb   -> After the first role on every node
/etc/puppet/modules/update-hooks/pre-node.rb    -> Before the first role on every node
